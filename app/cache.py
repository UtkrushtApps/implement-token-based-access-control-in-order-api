# This module manages the in-memory image cache for profile pictures.

# Module-level in-memory cache: { user_id: image_path }
image_cache = {}

def invalidate_cache_for_user(user_id: str):
    # TODO: Remove user's image from the cache
    pass
